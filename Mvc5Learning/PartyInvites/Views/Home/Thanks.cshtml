@model PartyInvites.Models.GuestResponse

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
    <title>Thanks</title>
    <style>
        body {
            background-color: #F1F1F1;
        }
    </style>
</head>
<body>
    @{
        try
        {
            //WebMail.SmtpServer = "smtp.exmail.qq.com";
            //WebMail.SmtpPort = 25;
            //WebMail.EnableSsl = true;
            //WebMail.UserName = "linzhenye@ocschina.com";
            //WebMail.Password = "910426@Yecheskel";
            //WebMail.From = "linzhenye@ocschina.com";
            //WebMail.Send("1098569376@qq.com", "RSVP Notification", Model.Name + " is " + ((Model.WillAttend ?? false) ? "" : "not") + "attending");
        }
        catch
        {
            @:<b>Sorry - we couldn't send the email to confirm you RSVP</b>
        }
    }
    <div class="text-center">
        <h1>Thank you, @Model.Name!</h1>
        <div class="lead">
            @if (Model.WillAttend == true)
            {
                @: It's great that you're coming. The drinks are already in the fridge
            }
            else
            {
                @: Sorry to hear that you can't make it, but thanks for letting us know.
            }
        </div>
    </div>
</body>
</html>
